# 개발 시작하기

접수·데몬 실행·복구·취소·조회 기능과 42개 시나리오 E2E가 구현되어 있다. 빠른 검증은 도구와 구조·단위 테스트를 확인한다. 기본 검증은 L2 E2E를 실행하고 L3 세 개를 건너뛰므로 전체 판정은 실패한다. 전체 완료 판정에는 실제 환경을 준비한 `--deep` 통과가 필요하며, 최신 결과는 [검증 현황](docs/roadmap.md)에 기록한다.

## 도구 준비

Go 1.22 이상과 golangci-lint v2, Bash, Git을 준비한다. full 이상은 race 검출기를 사용하므로 CGO를 사용할 수 있는 C 컴파일러도 필요하다. 현재 검증에 사용한 도구 버전은 Go 1.26.8, golangci-lint 2.13.2다. golangci-lint는 사용하는 Go 버전을 지원하는 바이너리를 사용한다. 도구가 없으면 검증은 실패하며 자동으로 건너뛰지 않는다.

저장소 루트에서 실행한다.

```sh
go mod download
scripts/verify.sh --fast
git config core.hooksPath .githooks
```

훅 설정은 해당 저장소의 Git 설정을 변경한다. pre-commit은 스테이징된 인덱스를 임시 디렉터리에 꺼내 빠른 검증을 수행한다. 작업 트리의 미스테이징 수정으로 잘못된 스테이징을 가리지 않는다.

## 변경하고 검증하기

먼저 [사용자 시나리오](docs/scenarios.md)에서 구현할 흐름을 고르고 [요구사항](docs/requirements.md)의 세부 계약과 [아키텍처](docs/architecture.md)의 계층 경계를 읽는다. test/의 해당 실제 CLI/데몬 E2E를 확인하고 변경에 맞춰 보강하며, 문서의 각 검증 줄을 같은 순서의 V 번호 단정문으로 연결한다.

```sh
scripts/verify.sh --fast
scripts/verify.sh
scripts/verify.sh --deep
```

| 명령 | 의미 |
| --- | --- |
| --fast | 포맷·게이트 자기검증·빌드·vet·게이트·lint·빠른 테스트. 개발 중과 pre-commit에 사용 |
| 옵션 없음 | 위 검사와 race를 포함한 E2E. 미구현·누락 실행은 실패 |
| --deep | 공통 검사 뒤 E2E를 E2E_DEEP=1, -count=1로 한 번 실행해 실제 외부 환경까지 확인 |

`--deep` 실행 전 [이슈 수정](test/testdata/issues/README.md), [자료 변환](test/testdata/conversion/README.md), [외부 게시](test/testdata/report/README.md)의 도구·인증 준비를 따른다. 기본 모드의 L3 건너뜀을 없애거나 미구현을 환경 부족으로 바꿔 통과시키지 않는다.

계층 경로·테스트 패키지·빌드 태그는 archgate.json에서만 관리한다. 검증 스크립트와 훅에 별도 목록을 만들지 않는다. 현재 빠른 테스트에는 게이트 자기검증이 포함되며, 빠른 검증의 성공이 제품 기능을 검증했다는 뜻은 아니다.

## 완료를 판단하기

시나리오 번호마다 TestScenario_SC_번호 함수 하나, 검증 줄마다 verify(t, "V-번호", ...) 호출 하나가 필요하다. verify는 실제 하위 테스트를 실행한다. 미구현은 SCENARIO_TODO가 포함된 skip으로 남겨 full 실패를 유지한다. 외부 환경이 부족한 경우는 구체적인 사유로 구분해 보고하며 문서 시나리오가 건너뛰어지면 전체 완료 검증은 실패한다.

구현자와 독립 리뷰어는 컨텍스트를 공유하지 않는다. 리뷰어는 기계 검사 이후 기대값이 검증 대상 코드에서 왔는지, 무동작이나 항상 참인 단정인지, 관측 밖의 부수효과가 있는지 확인한다. 핵심 변경을 제거·뒤집는 변형을 전체 테스트로 실행해 실제 실패를 확인하고 오케스트레이터가 중요한 주장을 재현한다. 구현·리뷰를 3회 반복해도 해결되지 않으면 사용자에게 올린다.

시나리오를 구현에 맞춰 약화하지 않는다. 제품 계약 변경이 필요하면 사용자 확인을 받고 시나리오와 요구사항 버전을 함께 갱신한다. 작업이 끝나면 [남은 작업](docs/roadmap.md)에 실제 완료 상태와 미확인 항목을 반영한다. 여러 에이전트가 공유하는 작업 트리에서는 변경 파일을 명시해 스테이징한다.
