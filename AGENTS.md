# 에이전트 작업 규칙

이 저장소는 todoable v1의 프로젝트 골격이다. 제품 기능은 미구현이며 문서의 CLI 예시를 이미 동작하는 기능으로 보고하지 않는다.

- 문서 위계는 docs/scenarios.md > docs/architecture.md·docs/requirements.md > 코드다. 완료 판정은 해당 시나리오 E2E 실제 통과로만 내린다. 시나리오 변경이 필요하면 멈추고 사용자에게 올린다.
- 검증 진입점은 scripts/verify.sh다. --fast는 개발·pre-commit용, 기본 실행은 E2E를 포함한 완료 판정용, --deep은 실제 외부 환경 검증용이다. 미구현 SCENARIO_TODO를 환경 부족으로 바꾸거나 숨기지 않는다.
- 계층·테스트 경로·빌드 태그는 archgate.json에서만 관리한다. 도구 자기검증이 제품 게이트 판정보다 앞이다. 문서와 설정이 어긋나면 상위 규범을 만족하도록 구현·설정을 수정한다.
- 메인은 시나리오 선택·위임·결과 판정·커밋을 맡고 구현자와 리뷰어를 분리한다. 리뷰어는 구현자의 대화 컨텍스트를 물려받지 않는다. 중요한 변형은 전체 테스트에서 검증하고 오케스트레이터가 핵심 주장을 재현한다. 구현·리뷰 3회 후에도 해결되지 않으면 사용자에게 올린다.
- 리뷰는 무동작 단정, 항상 참인 단정, 검증 대상에서 가져온 기대값, 관측 밖의 부수효과, 죽은 인자 필드 읽기, 부정확한 skip 사유를 확인한다. 기계가 확인한 존재·개수·실행을 의미적 정확성과 혼동하지 않는다.
- 공유 작업 트리에서 다른 에이전트 변경을 덮어쓰지 않는다. git add -A 대신 작업 파일을 명시한다. 훅은 인덱스를 검사하며 훅 통과를 제품 완료로 보고하지 않는다.
- 구현 후 docs/roadmap.md를 실제 검증 결과에 맞게 갱신한다. 제외·누락된 도구나 검증에는 근거를 남긴다.

처음 개발할 때는 CONTRIBUTING.md를 따른다. 상세 계약·예시는 docs/requirements.md, 원문 이관 대조는 docs/migration.md에 있다.
