# 남은 작업

SC-01~20·26·32~37의 27개 시나리오 E2E를 구현했다. 접수 검증은 실제 CLI와 SQLite를 대조하고 작업 명령이 접수 CLI에서 실행되지 않는지 확인한다. 실행 검증은 별도 `daemon`이 시작·종료검사와 전처리·에이전트·후처리를 수행하는 실제 기록과 산출물을 대조한다. 데몬은 여러 Step을 처리하며 기본 Run 2개·검사 4개 상한을 예약 트랜잭션에서 적용한다. 정지 미확인 Run 슬롯을 포함한 병렬 상한과 실행 의도 복구를 확인했다. 수동 해소·취소와 시간 예산 전체 계약은 해당 미구현 시나리오가 남아 있다.

현재 사용할 수 있는 명령은 `task register FILE`, `task update FILE --if-version N`, `task enable ID`, `task disable ID`, `run submit FILE`, `run show RUN_ID --json`, `schedule enable ID`, `schedule disable ID`, `schedule show ID [--json]`, `schedule submit ID --at TIME [--task-version N]`, `daemon`이며 전역 `--data-dir DIR`를 앞에 지정할 수 있다. 접수 코드 0은 영속 저장 성공이다. CLI는 작업 명령을 실행하지 않는다. 다른 명령과 schedule 외의 사람용 조회 출력은 미구현이다.

| 상태 | 작업 | 관련 시나리오 |
| --- | --- | --- |
| E2E 통과 | 같은 입력 동시 접수·재전송, 원자적 최초 Run·반복 예산 저장 | SC-01 |
| E2E 통과 | 응답 전 강제 종료 복구, 포화·비활성 중 중복, 갱신 뒤 재전송, JCS·메타데이터·해시 충돌·잘못된 JSON | SC-02·03·07·32 |
| E2E 통과 | 입력 키별 전체 반복 순서·차단 선두, 과거 입력 중복, 갱신 경합의 온전한 실행 스냅샷 | SC-04·05·06 |
| 부분 구현 | Task 활성 전환·과거 정의 보존. Task 전체 취소·조회는 미완 | SC-31 |
| E2E 통과 | 설치 설정·Task·입력 파싱, 원본/정규화 크기·깊이·기간 상한, 과거 버전 신규 접수 현 cap 검증 | SC-33 |
| 부분 구현 | SQLite WAL·FULL·외래키·고유 제약·즉시 트랜잭션 | SC-02~10, SC-42 |
| E2E 통과 | 최초 검사 성공, 마지막 호출 뒤 검사, 정상 비영 agent 결과, 환경·컨텍스트·argv·stdin·로그 | SC-11·12·13·34 |
| E2E 통과 | 서로 다른 Task의 같은 충돌 자원 배타·결과 불명 소유 유지 | SC-08 |
| E2E 통과 | 반복 자원 반환·ready 순서, after 실패 회차 이력·요약, 유한·무한 시작 대기와 반복 지연 | SC-10·14·15 |
| E2E 통과 | 시작 재확인 거짓·오류의 자원 반환과 변경 단계 실패 분류 | SC-35 |
| 부분 구현 | 연속 시간 예산·복구 예산 전체 계약 | SC-38 |
| E2E 통과 | 간격·cron·기간 보충·원자 커서 복구·활성화 이력·포화와 오류 격리 | SC-16~19, SC-36~37 |
| E2E 통과 | 정지 미확인 Run 슬롯 포함 전역 상한·변경 의도 강제 종료 복구 | SC-09·20 |
| 부분 구현 | PID 식별·정지 확인과 읽기 검사 복구. 수동 확인·취소 미완 | SC-21~24, SC-38~39, SC-42 |
| E2E 통과 | 출력 저장 상한·파이프 소비, 검사 timeout과 정지 미확인 검사 슬롯 유지 | SC-26 |
| 미구현 | 실행 중 설정 해시 비교·재시작 상한 적용, 저장 실패·로그 정리 | SC-25·27, SC-40 |
| 부분 구현 | JSON 오류와 접수·Run 조회. 나머지 CLI·데몬 경계 | SC-41 |
| 미구현 | 실제 이슈 수정·자료 변환·기간 리포트 | SC-28~30 |

YAML alias와 설치 `config.yaml`을 지원하며 파일·확장 크기·깊이 제한과 설치 cap을 적용한다. 신규 접수는 선택한 현재·과거 Task 버전을 현 cap으로 검증하고, 기존 접수의 재전송은 같은 접수로 돌려준다. 실행 중 데몬과 CLI의 설정 해시 비교, 재시작 시 실행·로그 상한 변경은 SC-40의 후속 범위다. 일정 정의·활성화·비활성화·기간 수동 접수는 구현했으나 취소·Task 조회는 SC-31의 미완 항목이다. 접수 환경 스냅샷을 실제 실행에 전달하는 계약은 SC-34에서 확인했다.

## 검증 기록 (2026-09-11)

- `scripts/verify.sh --fast`: 통과. 게이트와 빠른 테스트 결과이며 제품 전체 완료 판정은 아니다.
- `scripts/verify.sh`: SC-01·02·03·07·32와 각 V-01·V-02 통과(확인 15, 실패 0). 나머지 37개 시나리오는 `SCENARIO_TODO`로 남아 full은 실패한다.
- SC-02는 제품 훅 없이 가득 찬 stdout 파이프로 응답을 막고, 별도 SQLite 연결로 최초 커밋을 관측한 후 실제 CLI를 SIGKILL한다. 두 번의 재시작 제출에서 식별자·접수·Run·예산 보존과 외부 명령 미실행을 확인했다.
- SC-03은 기본 키별 상한 100건을 실제 CLI로 채워 코드 5를 확인하고 Task 비활성에서 코드 6을 확인한다. 두 상태 모두 기존 중복은 성공한다. 설치 설정은 이 검증에 필요하지 않아 미구현 상태를 유지했다.
- 중복 메타데이터 검사를 제거한 변형을 `go test -race ./...`로 실행하여 SC-07/V-02와 SC-32/V-01 실패를 확인한 후 복원했다. SC-07은 소모된 예산 상태를 주입해 재전송이 잔여 반복·사용 호출을 초기화하지 않는지도 확인한다.
- SC-07 독립 리뷰에서 버전 생략 재전송의 소모 예산 검증 누락을 확인해, 예산 소모 후 버전 생략·원버전 명시 각각 재전송 직후 잔여 반복 1·사용 호출 1을 확인하도록 보강했다. HEAD 기반 별도 복사에서 fast 통과, full 기존 5개 시나리오 통과·37개 TODO 실패를 확인했다. 버전 생략 때만 예산을 복원하는 변형을 전체 `go test -race -count=1 ./...`로 실행해 SC-07/V-02가 잔여 반복 2·사용 호출 0을 검출하는 실패를 확인했다.
- 중복 조회를 SQL `AND 0`으로 무력화한 뒤 `go test -race ./...` 실행: SC-01 두 검증과 저장소 중복 단정이 실패했다. 변형 후 원래 코드를 복원했다.
- 독립 리뷰에서도 중복 조회 무력화(`AND 0`)와 반복 잔여 예산을 0으로 만드는 변형을 전체 race 테스트로 실행해 SC-01 실패를 확인했다. 오케스트레이터는 중복 응답 때 잔여 예산을 1씩 늘리는 변형을 `go test -race ./...`로 실행해 두 검증의 실패를 재현했다.
- 저장소 단위 검증은 Run INSERT 실패를 주입해 접수·예산도 함께 롤백되는지 확인한다. JSON·Task 파서에는 퍼징 시드를 추가했다. 장시간 퍼징은 아직 수행하지 않았다.
- `go run golang.org/x/vuln/cmd/govulncheck@v1.8.0 ./...`: 알려진 도달 가능한 취약점 없음. 이번 의존성 도입 시 별도로 실행했으며 verify.sh의 고정 도구 준비·버전 관리 정책이 아직 없어 자동 게이트에는 추가하지 않았다.
- SQLite 드라이버는 CGO와 C 컴파일러가 필요하다. Linux arm64 게이트의 교차 타입 검사는 실제 arm64 SQLite 실행 검증을 대신하지 않는다. `--deep`과 실환경 L3 검증은 미구현이다.

후속 구현도 시나리오별 실제 E2E가 통과한 뒤에만 상태를 갱신한다. 실제 출시 전에는 Linux amd64와 arm64에서 실행하고 L3 결과와 건너뜀 사유를 기록한다.

## 실행 엔진 검증 기록 (2026-09-11)

- `scripts/verify.sh --fast` 통과(확인 90, 실패 0). `scripts/verify.sh`는 9개 구현 시나리오와 모든 V 검증 통과(확인 34, 실패 0), 나머지 33개 `SCENARIO_TODO` 때문에 실패한다.
- SC-11은 후처리를 외부 게이트로 멈춰 그 결과 저장 전에는 성공하지 않는지 확인하고, 후처리 정상 비영 종료는 `failed:after`인지 확인한다. SC-12·13은 실제 agent 산출물과 Step 순서를 대조해 마지막 호출 후 검사 및 비영 agent의 검사 진행을 확인한다.
- 실행기는 명령 시작 시 별도 SQLite 연결로 자신의 실행 의도·사용 호출 슬롯이 이미 커밋됐는지 관측한다. 모든 Step의 결과와 다음 단계는 한 트랜잭션이며 실행 중 트랜잭션을 유지하지 않는다. 프로세스 PID·그룹·부팅 및 시작 식별자를 저장한다.
- SC-34는 실제 Python 실행기의 외부 기록으로 모든 단계의 argv, stdin EOF, 예약 환경, 접수 스냅샷, Step별 0400 컨텍스트, 8 KiB 피드백, 비UTF-8 원본 로그를 확인한다. 누락 상속·예약 변수 거부와 시작검사·전처리·종료검사·agent·후처리 각각의 사라진 workdir 시작 실패도 검증한다. L2 실행기에는 `/usr/bin/python3`가 필요하며 없으면 실패한다.
- 마지막 호출 뒤 검사를 생략하는 변형을 전체 `go test -race -count=1 ./...`로 실행해 SC-12 두 검증 실패를 확인했다. 접수 환경 대신 데몬 환경을 사용하는 변형은 같은 전체 테스트에서 SC-34/V-01 실패로 검출했다. 변형은 별도 임시 복사에서 수행해 작업 트리 원본을 보존했다.
- 프로세스 어댑터 테스트는 리더 종료 후 남은 그룹 자손의 출력, 그룹 시간 초과 정지, 로그 합산 상한을 실제 프로세스로 확인한다. 이 테스트는 SC-20~26 등의 완전한 복구·상한 시나리오를 대신하지 않는다.
- 후속 과제: 직렬 데몬을 전역 Run·검사 상한과 공정한 자원 선점으로 확장, 반복 지연·시작 대기 기한·연속 Run 시간 계산, 과거 프로세스 정지 확인·검사 자동 복구·수동 해소·취소, storage_paused, 조회 전체 계약을 구현해야 한다. 현재 재시작은 결과 없는 Step을 보수적으로 `blocked:process_unknown`으로 남기며 과거 프로세스를 자동 종료하거나 재실행하지 않는다. 단순 반복 Run 생성만으로 SC-04·10·14 완료를 주장하지 않는다.

## 실행 엔진 독립 리뷰 보강 (2026-09-11)

- `/proc` 디렉터리 목록을 읽은 직후 다른 프로세스가 소멸하면 stat 읽기에서 `ENOENT`뿐 아니라 `ESRCH`도 반환한다. 후자를 데몬 전체 오류로 취급하던 경합을 수정했다. 회귀 검증은 두 소멸 오류 뒤에도 다음 생존 그룹을 찾는지 확인하며 접근 거부를 정지 증거로 오판하지 않는지도 확인한다.
- SC-34는 최초 `last_check: null`과 선점 후 시작검사·전처리·최초 종료검사의 시작검사 피드백을 포함해 7개 Step 컨텍스트를 각각 대조한다. 시작검사 피드백 저장을 삭제한 변형이 전체 race 테스트에서 SC-34/V-01 실패로 검출된다.
- SC-12 등을 공유하는 실행 검증은 독립적으로 제출한 fixture 제어값으로 각 Step의 정상 종료 코드와 호출 번호를 예측한다. 종료검사의 저장 결과를 항상 코드 0으로 바꾸는 변형이 전체 race 테스트에서 SC-12/V-01·V-02 실패로 검출된다.
- `scripts/verify.sh --fast` 통과(확인 91, 실패 0), full은 구현 9개 시나리오와 모든 V 통과(확인 34, 실패 0), TODO 33개로 실패 유지. 세 무력화 변형은 별도 복사에서 전체 `go test -race -count=1 ./...`로 검증하고 원본을 보존했다.

## 입력 순서·반복·시작 대기 검증 기록 (2026-09-11)

- `scripts/verify.sh --fast` 통과(확인 105, 실패 0). 기본 full은 SC-04·05·06·10·14·15를 포함한 15개 시나리오의 모든 V 통과(확인 52, 실패 0), 나머지 27개 TODO 때문에 실패한다. 빠른 검사 숫자에는 별도 변경으로 들어온 순수 일정 계산 테스트도 포함하며 일정 제품 시나리오 완료를 뜻하지 않는다.
- 입력 선두와 반복 지연이 해소된 뒤 최초 시작검사 시각·기한을 기록한다. 거짓 시작검사는 호출·반복을 소모하지 않고, 유한 기한 종료만 `skipped:start_timeout` 회차 종료와 후속 발행을 원자적으로 처리한다. SC-15는 실제 검사 반복과 무한 대기를 같은 경과 시간에서 대조하고 선행 접수·반복 지연을 기한에서 제외하는지 확인한다.
- 반복 회차는 종료 트랜잭션에서 자원·슬롯을 반환하고 새 Run과 대기 시각을 만든다. ready 진입마다 새 증가 순번을 저장하며 사용 가능한 자원 중 앞선 ready Run이 선점한다. SC-10은 A의 다음 반복과 이미 ready인 B가 같은 예약 트랜잭션에서 경쟁하도록 시작검사 없는 Task를 사용한다.
- SC-04는 A·B·C 각 3회차의 실제 변경 순서와 입력 보존, A의 확정 실패 뒤 후속 반복, 차단 선두 뒤 B·C의 시작검사 금지를 확인한다. SC-14는 실패·성공 회차와 agent 호출 예산, 저장 결과 및 누적 성공·실패 요약을 대조한다.
- SC-06은 갱신·접수 경합에서 선택된 온전한 정의와 모든 회차의 실제 명령·환경·입력·충돌 자원·시간/호출/반복 예산을 확인한다. 경합 뒤 별도 정의로 다시 갱신하여 어느 경합 순서에서도 현재 정의를 재조회하는 결함을 숨기지 않도록 했다.
- 선행 접수 조건 삭제, repeat_delay 무시, ready 순서를 접수 순서로 교체한 변형을 각각 별도 복사에서 전체 `go test -race -count=1 ./...`로 실행했다. 각각 SC-04·SC-15, SC-15, SC-10 실패를 확인했다. 최초 SC-10 fixture는 시작검사 시간 때문에 정렬 변형을 검출하지 못했으므로 경쟁 시점을 보강한 뒤 외부 순서 `[A1 A2]` 대 기대값 `[A1 B1]` 차이로 실패를 확인했다.
- 실행 예약 때 접수 스냅샷 대신 현재 Task 정의를 다시 읽는 변형도 전체 race 테스트에서 SC-06/V-01·V-02 실패로 확인했다. 변형 복사들은 독립 리뷰와 오케스트레이터 재현을 위해 보존했다.
- 후속 범위는 모든 병렬·정지 미확인 상한(SC-08·09), 검사 재확인 실패 자원 해제(SC-35), 연속 Run 시간과 복구 예산, 재시작 프로세스 식별·수동 해소·취소, 일정 접수와 설정이다. 이번 완료 판정에 포함하지 않았다.

## 입력 순서 독립 리뷰 보강 (2026-09-11)

- 독립 리뷰에서 반복 회차 산출물을 삭제해도 기존 테스트가 통과하는 관측 누락을 확인했다. SC-04·10·14는 모든 회차의 독립 외부 stage 순서와 agent 산출물 본문·호출 count, 후처리 산출물 한 회를 직접 읽어 확인한다. 후처리 산출물은 append 방식으로 남겨 같은 Run에서 숨은 재실행도 구별한다.
- SC-06은 선택된 Task 버전의 모든 반복 회차 산출물과 호출 count를 리터럴 기대값으로 확인하며 후처리 산출물 부재도 확인한다. SC-05는 과거 A 재전송 뒤 A·B 산출물 보존을 추가로 확인한다. 차단 선두의 후속 입력과 SC-15 거짓 시작조건은 전체 Run 디렉터리의 변경 산출물 부재를 확인하여 DB 기록 밖의 효과도 검출한다.
- 별도 복사에서 전체 `go test -race -count=1 ./...` 실행: 반복 회차 artifact 삭제는 SC-04·06·10·14 실패, 거짓 시작검사에 숨은 artifact 생성은 SC-04/V-02와 SC-15 양쪽 검증 실패로 확인했다. 리뷰어·오케스트레이터의 기존 변형 복사는 변경하지 않고 새 복사를 보존했다.
- `scripts/verify.sh --fast` 확인 106 통과, 기본 full 확인 52·실패 0·TODO 27로 기존 15개 시나리오의 모든 V 통과를 유지한다. 미구현 시나리오 때문에 full 전체 결과는 실패다.

## 공유 자원·검사 슬롯·출력 제한 검증 (2026-09-11)

- SC-08은 서로 다른 Task의 같은 충돌 키를 실제 배타 파일과 외부 진입·종료 기록, agent·after 산출물로 대조한다. 변경 결과가 불명인 첫 Run은 자원을 유지하며 후속 Task가 변경 명령을 시작하지 않는지 확인한다.
- 검사 슬롯을 Step 결과와 별도 영속 저장한다. 결과에 `process_unknown`을 기록해도 정지 미확인 검사 슬롯을 반환하지 않는다. SC-26은 검사 한 개가 불명일 때 독립 키의 출력 작업이 계속 실행되고, 4개가 불명이면 다섯 번째 검사를 시작하지 않는지 확인한다.
- SC-26의 실제 agent는 stdout·stderr에 각각 7 MiB 이상을 쓰고 마지막 산출물을 남긴다. 저장 합계 10 MiB와 원본 바이트 prefix, 잘림 표시를 확인하고 상한 뒤 파이프를 계속 소비해야 산출물까지 도달하도록 했다. 멈춘 검사는 200ms 제한 뒤 정지·`blocked:check_error`를 기록하며 별도 접수·조회·작업은 계속한다.
- 정지 불명은 검증용 자식이 새 세션에서 출력 파이프를 유지하는 고장 주입으로 만든다. 그룹 탈출을 정상 지원하는 기능은 아니다. fixture 정리는 정지 파일뿐 아니라 Run 디렉터리 소멸도 관측하고 실제 자식 종료를 확인한다.
- 규정된 foreground CLI인 `todoable daemon`을 지원하고 모든 실행 E2E가 이 명령을 사용한다. 기존 `daemon run`도 호환 별칭으로 유지한다.
- 이번 커밋 대상의 별도 복사에서 fast 확인 106 통과, full 확인 58·실패 0·TODO 25를 확인했다. SC-08·26 추가로 구현 17개 시나리오의 모든 V가 통과하며 전체 완료는 아니다. 미연결 OS 복구 도우미는 별도 후속 변경으로 남겨 이번 검증·완료 판정에 포함하지 않았다.
- 검사 불명 슬롯을 반환하는 변형, 변경 불명 자원을 반환하는 변형, 로그 상한 뒤 파이프 소비를 중단하는 변형을 각각 전체 race 테스트로 실행해 SC-26/V-02, SC-08/V-02, SC-26 양쪽 검증의 실패를 확인했다. 모든 변형 복사를 보존했다.
- 독립 리뷰에서 시작검사·before가 모두 없는 ready Run이 최초 종료검사의 슬롯 제한을 우회하는 결함을 확인했다. 실제 예약할 단계가 검사인지에 맞춰 조건을 수정하고, 검사 4개가 정지 불명인 상태에서 이 Task도 명령·산출물 없이 대기하는지 SC-26/V-02에 추가했다. 이전 조건을 복원한 별도 복사의 전체 race 테스트는 직접 종료검사 실행과 agent 호출을 검출하여 실패했다.
## 일정 통합 검증 기록 (2026-09-11)

- `scripts/verify.sh --fast` 통과(확인 106, 실패 0). 기본 full은 구현한 21개 시나리오의 모든 V 통과(확인 70, 실패 0), 나머지 21개 `SCENARIO_TODO` 때문에 실패한다. SC-31의 Task 전체 취소, SC-33·40의 설정·파서 전체 계약은 이번 완료 범위에 포함하지 않는다.
- 정기 발행은 수동 접수와 같은 저장 로직을 사용한다. 최신 미접수 기회, 접수와 최초 Run, 마지막 접수 시각을 같은 SQLite 트랜잭션에 기록하며 같은 Task·입력 키의 미완료 접수가 있으면 보충을 미룬다. SC-16은 긴 실제 프로세스를 대기시킨 뒤 최신 기회 하나의 접수와 생략 수, 기존 입력 보존을 대조한다.
- SC-17은 접수 커밋 직전과 직후 각각 실제 데몬을 SIGKILL한다. 재시작 후 CLI 응답, 별도 읽기 전용 SQLite 연결, 외부 실행 기록과 보고서로 접수·최초 Run·커서의 원자성과 중복 방지를 확인한다. E2E용 링크 플래그는 어댑터의 시각 관측과 커밋 경계 대기만 교체한다. 일반 바이너리는 해당 테스트 환경변수를 읽지 않으며 두 바이너리는 동일한 CLI·유스케이스·저장소·실행기 배선을 사용한다.
- SC-18·19는 수동 기간의 중복과 정확한 직전 기회, 뉴욕 DST의 없는 시각과 반복 시각, 시계 후퇴 시 커서 보존을 확인한다. 외부 Python 실행기는 고정 사건 자료를 전달받은 반개구간으로 필터링하여 보고서를 만들며 테스트는 독립적으로 지정한 행 목록·기간과 비교한다. 이 로컬 보고서 검증은 실제 외부 업무를 수행하는 SC-30의 L3 검증을 대신하지 않는다.
- SC-36은 허용·금지 cron, 향후 8년 유효성, 간격 최초 시각과 최대 8760h, 동일·변경 일정 갱신과 활성화 이력을 확인한다. 과거 버전의 수동 접수도 실제 보고서를 생성하며 비활성·재활성화는 기존 접수를 변경하지 않는다.
- SC-37은 환경 오류가 난 Task의 최신 기회·오류·생략 수를 유지하는 동안 다른 Task의 보고서가 완성되는지 확인한다. 별도 흐름에서는 DB 상태 주입 없이 실제 CLI로 기본 상한 1000건을 채우고, 실제 실행 한 건이 끝나 비운 슬롯을 같은 데몬이 재시도하는지 확인한다. 포화 상태에서 관측된 선행 접수 조회 지연을 해결하기 위해 Task·입력 키·상태·순번과 상태별 조회에 SQLite 인덱스를 추가했다.
- 일정 커서를 접수와 별도 트랜잭션으로 저장하는 변형은 전체 `go test -race -count=1 ./...`에서 SC-17/V-02의 커밋 직후 커서 누락으로 실패했다. 같은 입력 키의 미완료 접수 차단을 제거하는 변형도 같은 전체 테스트에서 SC-16/V-01·V-02와 SC-36/V-01 실패로 검출했다. 두 변형은 별도 복사에서 실행하여 원본을 보존했다.
- 설치 설정과 L3는 연결하지 않았다. 기본 상한의 실제 포화로 해당 L2 경계를 검증했으며 외부 계정·게시·실자료 작업은 후속 범위로 남긴다.

## 일정 통합 독립 리뷰 보강 (2026-09-11)

- SC-19/V-02는 실행 중 30초까지 관측한 최신 미접수 기회를 남긴 뒤 시계를 15초로 후퇴시키고 기존 실행을 완료한다. 15초와 29초에서 신규 접수·Run이 생기지 않으며 관측 시각·마지막 접수·미접수 기회·생략 수가 보존되는지 확인한다. 30초 회복과 다음 40초 기회는 각각 정확한 반개구간의 외부 보고서를 만들고 생략 수 1과 기존 입력을 유지한다.
- `scripts/verify.sh --fast` 통과(확인 106, 실패 0). 기본 full은 구현 21개 시나리오와 모든 V 통과(확인 71, 실패 0), 나머지 21개 `SCENARIO_TODO` 때문에 실패를 유지한다.
- 기존 pending 없는 후퇴 흐름이 놓친 `now.Before(st.Cursor.ObservedAt)` 방어 제거를 새 별도 복사에서 전체 `go test -race -count=1 ./...`로 실행했다. 보강한 SC-19/V-02는 15초 시점에 미래 30초 기회를 접수한 상태를 관측하여 실패한다. 제품 코드는 변경하지 않았다. 실환경 L3는 이번 로컬 시계 회귀 검증 범위에 포함하지 않는다.

## 실행·일정 병합과 중단 체크포인트 (2026-09-11)

- 실행 분기 `7b2b71c`와 일정 분기 `2f41574`를 병합했다. CLI 충돌은 일정 명령 진입과 `daemon`·`daemon run` 지원을 함께 보존했다. 일정 발행은 기존 실행 worker와 같은 저장소를 사용하며 별도 발행 루프를 추가한다. 검사 슬롯 예약·정지 불명 슬롯 유지와 입력·반복 산출물 보강 파일은 실행 분기 그대로 보존했다.
- 통합 인덱스를 꺼낸 별도 복사에서 `scripts/verify.sh --fast` 확인 106 통과, 기본 full 확인 77·실패 0·미구현 19를 확인했다. 구현 23개 시나리오의 모든 V가 통과하고 나머지 TODO 때문에 full 전체 결과는 실패다. L3 외부 업무 검증은 아직 실행하지 않았다.
- 사용자 요청으로 이 병합을 안정적인 중단 지점으로 삼는다. 미연결 프로세스 복구 도우미와 테스트는 별도 WIP 브랜치 `wip/recovery-process-checkpoint`에 보존하며 제품 완료 판정에 포함하지 않는다. SC-09·20·22·35·38을 포함한 후속 복구 구현은 시작하지 않고 남긴다.

## 후속 개발에서 결정하거나 추가할 것

- cron 파서를 구현할 때 퍼징을 추가하고, 기존 JSON·YAML 퍼징을 장시간 실행한다.
- 릴리스 버전 스탬핑과 --version은 기존 v1 CLI 계약에 없으므로 사용자와 계약을 확정한 후 추가한다.
- 커버리지 목표와 마이크로벤치마크는 현재 도입하지 않는다. 시나리오의 실효성을 우선하며 성능 병목이 관측되면 측정 범위를 정한다.

기능 제외 목록은 [요구사항](requirements.md) 1절에 유지한다. 이 목록은 새 제품 기능을 암묵적으로 추가하는 계획이 아니다.

## 실행 의도 복구·Run 슬롯·재확인 실패 (2026-09-14)

- SC-09·20·35의 실제 CLI/데몬 E2E를 추가했다. SC-09는 결과 불명 자원의 뒤에 대기 작업을 둔 채 독립 자원에서 실제 명령 구간 2개가 병렬 실행되는지 확인하며, 정지 미확인 Run이 있으면 그 슬롯을 포함해 상한 2를 지키는지 확인한다. 외부 효과와 후속 대기열 진행도 대조한다.
- SC-20은 agent 실행 중, 효과 직후, 프로세스 종료 뒤 결과 저장 전 각각 데몬을 실제 SIGSTOP·SIGKILL하고 재시작한다. 이전 그룹 정지, 호출 1회 보존, 변경 재실행 금지, 효과 파일 보존 및 동일 자원 대기를 확인한다. 실행 전 의도만 남은 경계와 결과 저장 완료 경계는 실제 저장소 예약·실행으로 구성하고 공개 daemon으로 복원한다. 이 주입은 실행 전 경계를 만들기 위한 것으로 별도의 CLI 강제 종료 검증을 대체하지 않는다.
- WIP 브랜치의 프로세스 도우미를 검토해 데몬 복구에 연결했다. 부팅·시작 식별값·그룹·Step 환경을 확인하고 PIDFD로 구성원을 고정해 TERM, 10초 후 KILL 순서로 정지한다. 식별 실패는 보수적으로 차단한다. PID가 기록되지 않은 변경 의도는 outcome_unknown으로 남기면서 정지 미확인 Run 슬롯을 유지한다.
- SC-35는 선점 후 시작 재확인 거짓, 정지 확인 오류, 정지 미확인 오류의 자원·슬롯 차이를 확인한다. before·agent·검사 시작 실패, 변경 단계의 시간 초과·시그널 결과 불명, stdout 성공 주장과 종료검사 실패를 실제 실행으로 대조한다.
- 별도 E2E는 결과 없는 시작·종료검사의 이전 실제 프로세스를 정지한 뒤에만 새 검사 의도를 예약하는지, 완료 결과와 이미 기록된 차단은 재시작만으로 재실행·해소되지 않는지 확인한다. SC-22의 수동 해소 요청과 SC-38의 연속 시간·재개·취소 계약은 아직 검증하지 않았으므로 해당 TODO는 그대로 유지한다.
- `scripts/verify.sh --fast` 통과(확인 122). 최종 `scripts/verify.sh`는 확인 108·실패 0·미구현 16이며 26개 구현 시나리오의 모든 V가 통과했다. 나머지 16개 `SCENARIO_TODO`로 full 전체는 실패한다. 로그는 `/tmp/recovery-fast.log`, `/tmp/recovery-full-final.log`에 남겼다. L3 실환경 검증은 이번 묶음에서 실행하지 않았다.
- 정지 확인된 변경 intent를 같은 단계로 자동 재실행하는 변형을 별도 복사 `/tmp/todoable-recovery-replay-kmawbi1j`에서 전체 `go test -race -count=1 ./...`로 실행했다. SC-20/V-01의 세 강제 종료 경계 모두 실패했고 프로세스 종료 뒤 복구에서는 사용 호출 2·잘못된 succeeded가 검출되었다. `/tmp/recovery-replay-mutation.log`에 실제 종료 1을 기록했다. 원 트리는 변형하지 않았다.

## 실행 의도 복구 독립 리뷰 보강 (2026-09-14)

- 독립 리뷰가 no-PID 변경 intent를 정지 확인된 결과로 바꾸어 Run 슬롯을 반환하는 변형이 전체 race 테스트를 통과하는 공백을 재현했다. 제품 수정 없이 SC-20/V-02를 보강해 호출 1회·충돌 자원·정지 미확인 Run 슬롯과 PID/그룹/부팅/시작 식별값 부재 및 Step의 process_unknown 기록을 직접 확인한다.
- 같은 자원 후속 입력이 대기하는 동안 독립 키 2개를 제출해 미확인 슬롯과 실제 실행 구간의 합이 2를 넘지 않는지 확인한다. 실제 실행이 끝나면 다음 독립 키가 진행하고 미확인 자원·슬롯은 계속 남는지 외부 산출물과 함께 대조한다.
- `scripts/verify.sh --fast` 확인 122 통과. 기본 full은 확인 108·실패 0·TODO 16이며 기존 26개 구현 시나리오의 모든 V 통과를 유지한다. 로그: `/tmp/recovery-reviewfix-fast.log`, `/tmp/recovery-reviewfix-full.log`.
- 동일 슬롯 반환 변형을 `/tmp/todoable-recovery-nopid-fix-hybbmhjr` 별도 복사에서 전체 `go test -race -count=1 ./...`로 재현했다. SC-20/V-02/saved-false가 held=0·result=interrupted를 검출하여 종료 1로 실패한다. 로그 `/tmp/recovery-reviewfix-mutation.log`. 원 트리에 변형을 적용하지 않았다.

## SC-33 설정·Task·입력 파싱 (2026-09-14)

- `config.yaml`의 요구사항 11절 기본값, 전역 필드와 기간 cap을 읽고 알 수 없는 키·잘못된 값·중복 필드·파일 1 MiB 초과를 코드 2로 거부한다. CLI의 Task·입력 읽기 제한과 접수 개수 제한은 설치 설정을 사용한다. 데몬의 설정 해시 비교와 재시작 후 실행·로그 상한 적용 및 기존 버전 신규 접수 검증은 SC-40의 후속 범위다.
- Task YAML은 정상 alias(문자열 키 포함)를 확장하며 순환 alias·중복 키·태그·merge key를 거부한다. 확장 중 바이트 예산과 깊이를 제한하여 거대한 중간 객체를 만들기 전에 중단한다. 원본과 JSON 표현 및 JCS 결과 크기를 검사한다. 기간 기본값도 설치 cap보다 크면 조용히 줄이지 않고 거부한다.
- 입력 깊이는 접수 envelope를 제외하고 최대 64로 검사한다. 깊이 64 입력의 중복 해시도 같은 경계를 사용한다. 원본 JSON과 JCS 결과 크기를 각각 확인하며 제출 파일 전체에는 추가 16 KiB만 허용한다.
- SC-33은 실제 CLI로 정상 alias, 기본값, manifest와 input 정확 바이트 경계, input 깊이 64/65, 기간 cap 경계, 정규화 크기 초과를 검증한다. 각 거부 전후 Task 버전 정의와 접수·입력·스냅샷·해시·Run·호출/반복 예산이 동일한지도 대조한다. 외부 실행이 발생하지 않는지 별도 실행 기록을 관측한다.
- 빠른 검증은 확인 106·실패 0이다. 기본 full은 확인 80·실패 0·미구현 18로 SC-33의 모든 V를 포함한 구현 24개 시나리오가 통과하고, 남은 `SCENARIO_TODO` 때문에 전체 실패를 유지한다. 외부 환경을 사용하는 L3는 이번 파싱 계약 검증 범위에 포함하지 않았다.
- 정규화 input 크기 검사만 제거한 별도 복사 `/tmp/todoable-config-mutation`에서 `go test -race -count=1 ./...`를 실제 실행했다. 원본 10바이트인 `{"x":1e-6}`의 JCS 결과가 설치 상한 10바이트를 넘는데 접수가 성공하여 SC-33/V-01이 실패했다. 전체 실행 로그는 `/tmp/config-mutation.log`에 보존한다.

## SC-33 1차 독립 리뷰 보강 (2026-09-14)

- 독립 리뷰에서 이미 등록된 Task가 낮아진 설치 cap을 신규 접수에서 우회하는 결함을 재현했다. 신규 접수는 현재 선택된 버전의 반복·호출·모든 기간 cap과 최종 입력 크기를 다시 검증한다. 중복 접수 판정은 그보다 앞서 유지하여 cap 변경이 기존 접수의 재전송을 새 실행이나 거부로 바꾸지 않는다.
- SC-33 E2E는 현재 버전과 명시 버전의 cap 위반 거부, 실제 과거 버전 1의 거부와 cap을 만족하는 현재 버전 2의 접수, 기존 접수 재전송 보존을 대조한다. 각 거부 전후 전체 Task 버전 정의와 접수·예산이 동일한지도 확인한다.
- 기존 테스트가 놓쳤던 manifest 자체의 JCS 확장을 추가했다. schedule.input의 `1e-6`이 정규화 후 네 바이트 늘어나는 원본은 원본 크기 상한에서는 거부하고 정규화 결과 정확 크기 상한에서는 허용한다. `caps.repeat_delay`, `caps.start_poll_every`, `caps.start_wait_timeout`도 설치 상한 1s에서 Task의 정확 1s 허용과 1s1ns 거부를 검증한다.
- 신규 접수의 Task cap 재검증 제거 변형(`/tmp/todoable-config-revalidation-mutation`), manifest 정규화 크기 검사 제거 변형(`/tmp/todoable-config-manifest-mutation`), 세 기간 cap을 기본값으로 고정하는 변형(`/tmp/todoable-config-duration-mutation`)을 각각 전체 `go test -race -count=1 ./...`로 실행한다. 결과 로그는 `/tmp/config-revalidation-mutation.log`, `/tmp/config-manifest-mutation.log`, `/tmp/config-duration-mutation.log`에 보존한다.
- 1차 수정 최종 검증: fast 확인 106·실패 0, full 확인 80·실패 0·미구현 18. 위 세 변형의 전체 race 실행은 모두 SC-33/V-01에서 실패했다. manifest 변형은 초과 정의 등록이 성공했으며, 기간 cap 변형은 검증 코드 2 대신 정의 충돌 코드 3까지 진행했다. 제품의 전체 완료는 여전히 남은 TODO 때문에 아니다.

## 복구·설정 파서 통합 (2026-09-14)

- 독립 리뷰가 승인한 복구 `b59acae`와 설정 파서 `c6e0a5f`·리뷰 보강 `8c453c0`을 병합했다. 충돌은 이 문서의 추가 기록 한 곳이며 양쪽 검증 이력을 모두 보존했다. 제품 코드와 시나리오 테스트는 충돌 없이 합쳐졌고 실행 의도 복구·미확인 슬롯과 설치 cap·정규화 크기 계약을 함께 검증했다.
- 별도 통합 작업트리 `/tmp/todoable-config-recovery`에서 `scripts/verify.sh --fast` 확인 122·실패 0, 기본 `scripts/verify.sh` 확인 111·실패 0·미구현 15를 실제 확인했다. 구현 27개 시나리오의 모든 V가 통과했으며 남은 15개 `SCENARIO_TODO` 때문에 full 전체 결과는 실패다. 로그: `/tmp/config-recovery-integration-fast.log`, `/tmp/config-recovery-integration-full.log`.
- SC-40의 설정 해시·재시작 상한 배선 진행분은 별도 stash에 보존하여 이번 통합에 포함하지 않았다. 수동 해소·취소·전체 시간 예산, 저장 실패·완료 로그 정리, 남은 CLI·백업과 L3 실제 업무 검증도 해당 시나리오 TODO로 유지한다. 이번 통합은 새 외부 환경 작업을 실행하거나 L3를 로컬 테스트로 대체하지 않았다.

## 독립 충돌 키 검증의 ready 순서 경합 수정 (2026-09-14)

- 전체 검증 중 SC-09/V-02가 일시 실패했다. 그 최초 실패의 상세 원인은 보존된 testreport 출력만으로 단정하지 않는다. 별도 독립 재현에서는 free-0의 시작검사를 200ms 늦추자 free-1이 먼저 ready가 되었는데, 테스트가 free-0의 진입을 기다리면서 free-1의 gate를 열지 않아 교착하는 검증 경합을 확인했다. 제품의 ready 순서는 정상이다.
- SC-09는 제출 순서 대신 실제 before에 진입한 free Run 집합이 가용 슬롯 수에 도달하는지 관측한다. 모든 gate를 해제한 뒤 각 Run 완료와 외부 산출물을 확인하고, 전체 before/after_done 이벤트의 동시 구간 수가 정지 미확인 슬롯을 포함한 상한 2를 넘지 않는지 추가로 대조한다. 병렬성·같은 자원 대기·정지 미확인 슬롯 유지 단정은 유지했다.
- `scripts/verify.sh --fast` 확인 122 통과. 기본 full은 확인 111·실패 0·TODO 15이며 현재 27개 구현 시나리오의 모든 V가 통과한다. 로그 `/tmp/sc09-order-fast.log`, `/tmp/sc09-order-full.log`; 전체 full은 남은 TODO로 실패한다.
- free-0 시작검사에 200ms 지연만 추가한 `/tmp/todoable-sc09-delayed-5qani39g` 별도 복사에서 전체 `go test -race -count=1 ./...`가 종료 0으로 통과했다(로그 `/tmp/sc09-order-delayed.log`). 병렬 순서 교란을 허용하는지 확인한 것이며 TODO 시나리오의 제품 완료를 뜻하지 않는다.
- 전역 Run 예약 상한을 2에서 3으로 올린 `/tmp/todoable-sc09-overcap-hrlj05o6` 변형은 같은 전체 race 실행에서 SC-09/V-01이 실제 구간 3 대 허용 2, V-02가 2 대 허용 1을 검출해 실패한다. SC-20/V-02의 no-PID 슬롯 검증도 함께 실패했다. 로그 `/tmp/sc09-order-overcap.log`, 실제 종료 1. 제품 코드는 원 worktree에서 변경하지 않았다.
